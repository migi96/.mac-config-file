return {
	"ThePrimeagen/vim-be-good",
	event = "VeryLazy", -- Loads the plugin after everything else for better startup performance
	version = "*", -- Use for stability; omit to use `main` branch for the latest features
	config = function()
		-- Optional: Add any configuration settings for vim-be-good here
		-- For example, keybindings or specific settings related to the plugin.
	end,
}
